# Architecture Guard Scan Report — 2026-05-21

> Post-008-SG closure verification. Triggered by `/speckit.architecture-guard.architecture-verify`.

## Scan Scope

- All `Dockerfile` instances (15 files)
- `docker-compose.prod.yml` network isolation and port exposure
- `opensips/opensips.cfg.tpl` for module validity and auth contracts
- Secret hygiene (tracked files)
- SHA pinning compliance

## Results Summary

| Check | Status | Notes |
|---|---|---|
| No `db_mysql` references | PASS | Zero findings across codebase |
| No `sanity` module in OpenSIPS config | PASS | Only in `orchestrate-deploy.sh` as negative gate |
| No PostgreSQL/Asterisk host-published ports | PASS | `docker-compose.prod.yml` exposes only OpenSIPS (5060/udp/tcp, 5061/tcp), RTPengine (10000-20000/udp), OCP localhost-only (127.0.0.1:8080) |
| `calculate_ha1 = 0` | PASS | `opensips.cfg.tpl:90` |
| `password_column = "ha1"` | PASS | `opensips.cfg.tpl:92` |
| No `rtpengine_manage()` | PASS | Explicit `rtpengine_offer/answer/delete` used |
| `topology_hiding("C")` baseline | PASS | Lines 553, 690 in `opensips.cfg.tpl` |
| `mf_process_maxfwd_header(70)` | PASS | Line 280 |
| Integer algorithm in `ds_select_dst` | PASS | `ds_select_dst(..., 4, "f")` lines 534, 880 |
| SHA-pinned base images | **FAIL → PASS** | `certbot/certbot:latest` and `tailscale/tailscale:stable` were mutable |
| Secrets not tracked by git | PASS | `secrets/`, `.env*` excluded; `git ls-files` returns zero secrets |
| `cap_drop` + `security_opt` present | PASS | All services in `docker-compose.prod.yml` declare hardening |
| RTPengine control socket binding | PASS | `--listen-ng=${RTPENGINE_INTERNAL_IP}:22222` on `sip_internal` only |

## Violations Found and Remediated

### V-001: certbot Dockerfile uses `:latest`
- **File**: `docker/certbot/Dockerfile:1`
- **Severity**: P0 (constitution.md Docker contracts)
- **Before**: `FROM certbot/certbot:latest`
- **After**: `FROM certbot/certbot@sha256:0107d084c225631fc64a8313e19adb07275f7296fde338f7dfa93986c80b2e3e`
- **Commit**: `21d982e`

### V-002: tailscale-cert Dockerfile uses `:stable`
- **File**: `docker/tailscale-cert/Dockerfile:1`
- **Severity**: P0 (constitution.md Docker contracts)
- **Before**: `FROM tailscale/tailscale:stable`
- **After**: `FROM tailscale/tailscale@sha256:dbeff02d2337344b351afac203427218c4d0a06c43fc10a865184063498472a6`
- **Commit**: `21d982e`

## Remaining Non-Blocking Items

| Item | Severity | Context |
|---|---|---|
| Cloudflare TLS 1.0/1.1 enabled | P2 (edge config) | Documented in `docs/security/evidence/008-ssl-labs-analysis-20260521.md`; remediation requires Cloudflare dashboard change, not code |
| OCP v9 stub PHP files | Accepted deviation | Already tracked in `architecture_constitution.md` |

## Conformance Statement

Architecture is conformant after remediation of V-001 and V-002. No P0 blocking violations remain. Next scan recommended before next feature implementation or base image updates.

---
**Scanner**: Kimi Code CLI (Architecture Guard)  
**Constitution version**: 1.1.0  
**Constitution file**: `.specify/memory/constitution.md` + `.specify/memory/architecture_constitution.md`
