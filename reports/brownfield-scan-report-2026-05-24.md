# Brownfield Scan Report

**Scan Metadata:**
- Scope: all
- Branch: main
- Commit: 1631b0b fix(postgres): add user 999:999 to prevent cap_drop permission issues
- Files scanned: 34,285
- Date: 2026-05-24T01:41:46-03:00

---

## Findings

| ID | Category | Severity | File | Line | Finding | Recommendation |
|----|----------|----------|------|------|---------|----------------|
| B1 | Spec Drift | CRITICAL | docker-compose.vps.yml | 65 | RTPengine control socket --listen-ng=0.0.0.0:22222 binds to all interfaces. AGENTS.md Section 4 requires binding only to sip_internal address. | Change to --listen-ng=${RTPENGINE_INTERNAL_IP}:22222 to match docker-compose.yml and docker-compose.prod.yml. |
| B2 | Security Surface | HIGH | secrets/server.crt | N/A | Self-signed server certificate expires May 18 00:57:05 2027 GMT (~1 year). No documented auto-rotation for self-signed certs. | Add cert expiry monitoring alert; document manual rotation procedure or integrate with certbot/tailscale-cert for auto-renewal. |
| B3 | Technical Debt | MEDIUM | scripts/test-invite-407.sh | 4, 29-34 | Hard-coded Docker network IP 172.19.0.4 used in INVITE test script. Breaks if network CIDR changes. | Parameterize via TEST_IP env var with dynamic discovery (e.g., docker inspect or compose network query). |
| B4 | Technical Debt | LOW | .specify/extensions/** | various | Template TODO/FIXME markers in committed speckit extension files (e.g., agent-governance templates). | Out of project core scope; consider .gitignore for extension template artifacts or resolve upstream. |

---

## Summary by Severity

| Severity | Count |
|----------|-------|
| CRITICAL | 1 |
| HIGH | 1 |
| MEDIUM | 1 |
| LOW | 1 |

---

## Verified Non-Findings (Intentionally Excluded)

| Check | Result | Evidence |
|-------|--------|----------|
| Forbidden DB modules (db_mysql, db_sqlite) | Pass | Zero references in opensips configs, Dockerfiles, or compose files. |
| sanity module usage | Pass | Not referenced in any OpenSIPS config or Dockerfile. |
| Bare-metal install instructions | Pass | No apt-get install opensips or yum install opensips outside Dockerfiles. |
| Network naming (snake_case) | Pass | Networks use sip_edge, sip_internal, db_internal, metrics_host. |
| Asterisk/PostgreSQL host ports | Pass | No ports: on asterisk-pbx or postgres services. |
| Loopback RTPengine control | Pass | docker-compose.yml and docker-compose.prod.yml use ${RTPENGINE_INTERNAL_IP}:22222. |
| Plaintext password fields | Pass | subscriber uses HA1 only; calculate_ha1=0 per AGENTS.md. |
| maxfwd/rr modules | Pass | Both maxfwd.so and rr.so loaded in opensips.cfg.tpl. |
| Dispatcher header fallback | Pass | No hard-coded ds_select_dst(1, ...) found. |
| Rate limiting (pike/ratelimit) | Pass | pike.so and ratelimit.so loaded with modparam config in opensips.cfg.tpl. |
| Healthchecks | Pass | 14 healthcheck definitions in docker-compose.yml. |
| Restart policies | Pass | 15 restart policy definitions in docker-compose.yml. |
| .env in .gitignore | Pass | .env, .env.*, .env.local, .env.production all excluded. |
| RTPengine public ports (prod) | Pass | Only 10000-20000:10000-20000/udp exposed (expected RTP range). |

---

## Top 3 Action Items

1. Fix CRITICAL: RTPengine control exposure in docker-compose.vps.yml
   - Change line 65 from --listen-ng=0.0.0.0:22222 to --listen-ng=${RTPENGINE_INTERNAL_IP}:22222
   - This aligns with canonical spec Section 11 and AGENTS.md Section 4.

2. Address HIGH: Certificate expiry monitoring
   - secrets/server.crt expires in ~1 year (May 2027).
   - Add Prometheus alert rule or Grafana annotation for cert expiry.
   - Document rotation runbook if auto-renewal is not yet configured.

3. Address MEDIUM: Parameterize test script IP
   - scripts/test-invite-407.sh should not assume 172.19.0.4.
   - Use docker compose exec network inspection or pass TEST_IP from CI/caller.

---

*Report generated by speckit-brownfield-scan. Strictly read-only scan.*
