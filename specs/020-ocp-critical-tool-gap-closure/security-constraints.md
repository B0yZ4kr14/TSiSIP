# Security Constraints — Feature 020: OCP Critical Tool Gap Closure

> Generated by speckit-architecture-guard-governed-tasks workflow.
> Reviewed against: .specify/memory/security_constitution.md v1.0.0
> Date: 2026-05-19

---

## Executive Summary

Feature 020 security posture: **CONDITIONAL PASS**
- 10/10 security requirements (R1-R10) pass
- 3 medium-severity findings identified (1 new, 2 pre-existing brownfield)
- 0 critical or high-severity findings
- All findings are non-blocking for Feature 020 closure but must be addressed in remediation cycle

---

## Trust Boundary Validation

| Boundary | Rule | Status | Evidence |
|---|---|---|---|
| Public SIP Edge | Authenticate all non-OPTIONS SIP requests | N/A | OCP is HTTP, not SIP |
| Internal SIP | Topology hiding mandatory | N/A | No SIP forwarding in OCP |
| Database | No container bypasses OpenSIPS to write subscriber data | Pass | OCP writes to dialplan/domain only; not subscriber |
| Control Plane | OCP proxied via Nginx; no direct container exposure | Pass | OCP bound to 127.0.0.1:8084 when userland-proxy=true; VPS uses container bridge IP via nginx |
| Observability | Metrics endpoints must not leak tenant-scoped data | Pass | Statistics show aggregate counts only |

---

## Authentication & Authorization Validation

### SIP Digest Authentication
- N/A for OCP (web-based auth, not SIP Digest)

### OCP Web Authentication
| Standard | Requirement | Status | Evidence |
|---|---|---|---|
| bcrypt hashing | password_hash() for OCP accounts | Pass | Existing common/config.php |
| Account lockout | 5 failed attempts trigger lockout | Pass | Existing login.php |
| Forced password change | New accounts require change on first login | Pass | checkPasswordChange() called |
| Role hierarchy | readonly < user < assistant < dentist < devops < admin | Pass | Enforced in all 6 tools |
| Session security | cookie_secure enabled when HTTPS detected | **Fail** | No session.cookie_secure set |
| CSRF | All state-changing forms require CSRF token validation | Pass | validateCsrfToken() on all POST |

### Role Enforcement Matrix

| Tool | Minimum Role | Admin-Only Features | Status |
|---|---|---|---|
| dialog.php | devops | None (read-only) | Pass |
| mi-commands.php | devops | dlg_end_dlg, tls_reload | Pass |
| statistics.php | devops | None | Pass |
| dialplan.php | devops | None | Pass |
| domains.php | devops | None | Pass |
| tls-management.php | devops | tls_reload trigger | Pass |

---

## Data Isolation & Privacy Validation

| Rule | Status | Evidence |
|---|---|---|
| All tables include tenant_id foreign key | N/A | dialplan and domain tables are global, not tenant-scoped |
| OCP queries filter by tenant_id | N/A | Admin tools are global; no multi-tenant filtering needed |
| Cross-tenant query leakage is P0 | N/A | Not applicable to global admin tables |

Note: dialplan and domain are global OpenSIPS configuration tables. They do not have tenant_id columns by design (stock OpenSIPS schema).

---

## Secrets Management Validation

| Rule | Status | Evidence |
|---|---|---|
| No secrets committed to Git | Pass | secrets/ and .env* are gitignored |
| Secrets injected at runtime | Pass | Docker secrets or env vars |
| No secrets in PHP source | Pass | All credentials from env/secrets |

---

## Secure-by-Design Validation

### Header Sanitization
- N/A for OCP (SIP header sanitization is OpenSIPS responsibility)

### Output Sanitization
| Tool | htmlspecialchars() | Status |
|---|---|---|
| dialog.php | All displayed fields | Pass |
| mi-commands.php | Command output, error messages | Pass |
| statistics.php | Metric labels (server-side), D3 data (JSON-encoded) | Pass |
| dialplan.php | All table cells, form values | Pass |
| domains.php | All table cells, form values | Pass |
| tls-management.php | Certificate fields, error messages | Pass |

### Container Hardening
- N/A (no new containers introduced)

---

## API & Integration Security Validation

### MI HTTP Interface
| Control | Requirement | Status | Evidence |
|---|---|---|---|
| Whitelist | Only pre-approved commands | Pass | $miWhitelist array |
| Timeout | cURL timeout prevents indefinite hangs | Pass | 5s (stats), 10s (MI) |
| Error handling | No stack traces leaked | Pass | User-friendly error messages |
| Input validation | hash_entry/hash_id validated for dlg_end_dlg | Pass | Empty check before execution |

### TLS Requirements
| Control | Requirement | Status | Evidence |
|---|---|---|---|
| TLS reload | Requires admin role | Pass | requireRole('admin') |
| Audit logging | All reload attempts logged | Pass | logAuditEvent() on success and failure |

---

## Audit, Logging & Monitoring Validation

### Audit Event Coverage

| Event | Table | Logged On | Gap |
|---|---|---|---|
| MI command execution | auth_audit_log | Success, whitelist rejection, role rejection, connection failure | None |
| TLS reload | auth_audit_log | Success, failure, CSRF failure | None |
| Dialplan CREATE | auth_audit_log | Success only | **Missing failure logging** |
| Dialplan UPDATE | auth_audit_log | Success only | **Missing failure logging** |
| Dialplan DELETE | auth_audit_log | Success only | **Missing failure logging** |
| Domain CREATE | auth_audit_log | Success only | **Missing failure logging** |
| Domain UPDATE | auth_audit_log | Success only | **Missing failure logging** |
| Domain DELETE | auth_audit_log | Success only | **Missing failure logging** |

### Gap Analysis
- **Gap G1**: CRUD failure audit logging missing in dialplan.php and domains.php
  - Severity: MEDIUM
  - File: web/dialplan.php lines 61-63, 106-108; web/domains.php lines 45-47, 71-73
  - Fix: Add logAuditEvent() in PDOException catch blocks

---

## Findings Summary

| ID | Finding | Severity | Constitution Ref | Status |
|---|---|---|---|---|
| SEC-020-F01 | CRUD failure audit gap | MEDIUM | Security Constitution Section 7 | Open |
| SEC-020-F02 | Missing security headers (CSP, X-Frame-Options) | MEDIUM | Security Constitution Section 5 | Open (pre-existing M2) |
| SEC-020-F03 | Session hardening gap | MEDIUM | Security Constitution Section 2 | Open (pre-existing M3) |

---

## Recommendations

1. **Immediate (before next feature)**: Fix SEC-020-F01 by adding logAuditEvent() in PDOException catch blocks
2. **Short-term (next remediation cycle)**: Fix SEC-020-F02 and SEC-020-F03 via common/config.php and common/header.php
3. **Ongoing**: Add automated test that verifies audit log entry exists for every failed CRUD operation

---

## Sign-Off

- Security Review: Complete
- Blocking findings: 0
- Non-blocking findings: 3 (all MEDIUM)
- Next action: Generate architecture refactor tasks for remediation
