# TSiSIP Brownfield Scan — Final Focused Verification

**Date:** 2026-05-24  
**Scope:** `opensips,docker`  
**Format:** Markdown  
**Severity Filter:** `critical,high`  
**Focus:** Verification of brownfield fixes B1–B6 and two targeted post-remediation items

---

## Executive Summary

**All targeted critical/high findings resolved.**

All six brownfield fixes (B1–B6) remain in place, and the two additional targeted items identified in the previous verification scan (`brownfield-scan-verification-2026-05-19.md`) have been correctly remediated.

---

## Targeted Item Verification

### Item 1: RTPengine Control Socket Binding (`docker-compose.yml`)

- **Location:** `docker-compose.yml:152`
- **Expected:** `--listen-ng=${RTPENGINE_INTERNAL_IP}:22222` (not `0.0.0.0`)
- **Status:** ✅ **FIXED**
- **Evidence:**
  ```yaml
  - --listen-ng=${RTPENGINE_INTERNAL_IP}:22222
  ```
  The development profile now aligns with `docker-compose.prod.yml:67` and `docker-compose.vps.yml:106`.

### Item 2: Dispatcher Probing Threshold Comment (`opensips.cfg.tpl`)

- **Location:** `opensips.cfg.tpl:213`
- **Expected:** Comment reads `ds_probing_threshold=2` (not `5`)
- **Status:** ✅ **FIXED**
- **Evidence:**
  ```c
  # Global ds_probing_threshold=2 applies; setid-specific destinations are
  ```
  This matches the actual `modparam` at `opensips.cfg.tpl:113`.

---

## B1–B6 Fix Verification Matrix

| ID | Fix Description | Status | Evidence |
|---|---|---|---|
| **B1** | RTPengine no longer on `sip_edge` network | ✅ **VERIFIED** | `rtpengine` service networks list **only** `sip_internal` in all three Compose files. |
| **B2** | `topology_hiding_match()` added before `loose_route()` | ✅ **VERIFIED** | `opensips.cfg.tpl:247` calls `topology_hiding_match()` before `loose_route()` at line 250 inside the `has_totag()` / in-dialog branch. |
| **B3** | Auth credential stripping moved to `route[RELAY]` | ✅ **VERIFIED** | `remove_hf` for auth headers appears **only** in `route[RELAY]` (`opensips.cfg.tpl:920–921`). `route[SANITIZE]` (`opensips.cfg.tpl:404`) contains a confirming comment that the move was intentional. |
| **B4** | `rtpengine_answer()` has canonical flags | ✅ **VERIFIED** | `onreply_route` at `opensips.cfg.tpl:363` uses `rtpengine_answer("replace-origin replace-session-connection")`. |
| **B5** | RTP port range is `10000-20000` | ✅ **VERIFIED** | `docker-compose.yml:149` maps `10000-20000:10000-20000/udp`; `docker-compose.prod.yml:64` does the same. Command-line args `--port-min=10000 --port-max=20000` are present. |
| **B6** | `ds_probing_threshold=2` | ✅ **VERIFIED** | `opensips.cfg.tpl:113` contains `modparam("dispatcher", "ds_probing_threshold", 2)`. |

---

## Conformance Statement

- **Docker-first rule:** ✅ All OpenSIPS and RTPengine runtimes remain containerized. No bare-metal or VM-first instructions detected.
- **PostgreSQL-only rule:** ✅ Only `db_postgres.so` is loaded; no MySQL/MariaDB references found.
- **Topology hiding baseline:** ✅ `topology_hiding("C")` is used consistently.
- **Explicit RTPengine functions:** ✅ `rtpengine_offer()`, `rtpengine_answer()`, and `rtpengine_delete()` are used explicitly; `rtpengine_manage()` is absent.

---

## Conclusion

All targeted critical/high findings from the brownfield remediation cycle have been verified as resolved. No regressions detected in the `opensips` or `docker` scopes.

*Report generated by brownfield-scan focused verification on B1–B6 + targeted post-remediation items.*
